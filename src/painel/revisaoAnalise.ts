/**
 * A introdução revisada na PRÉVIA de quem revisa.
 *
 * A quebra em parágrafos e a substituição da leitura automática vivem em
 * `src/reports/analisePublicada.ts`, que é a mesma regra usada na página do
 * cliente. Duas cópias divergiriam, e a divergência aqui tem nome: o revisor
 * aprovaria um texto e o cliente leria outro.
 */
export { afirmacoesDaIntroducao as afirmacoesDaIntroducaoRevisada } from '../reports/analisePublicada';
